import trooper from "$lib/assets/trooper.webp";

export default function test(): string {
    return `<img src='${trooper}' alt='stormtrooper' class="w-96" />
Nothing to test here, move along, move along.
\n`;
}