import simply from "$lib/assets/one_does_not_simply.webp";

export default function exit(): string {
    return `<img src='${simply}' alt='one does not simply' class="w-96" />
One does not simply exit MESh.
Have you tried REISUB?
\n`;
}